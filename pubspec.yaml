name: flutter_address_input
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.1.4 <4.0.0'

dependencies:
  cloud_firestore: ^4.13.2
  firebase_core: ^2.23.0
  flutter:
    sdk: flutter
  flutter_hooks: ^0.20.3
  gap: ^3.0.1
  hooks_riverpod: ^2.4.8
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
